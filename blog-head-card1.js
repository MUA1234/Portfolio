// You can add any JavaScript functionality here if needed
// For example, dynamically changing the paragraph text
document.addEventListener("DOMContentLoaded", () => {
    const paragraph = document.getElementById("long-paragraph");
    paragraph.textContent = "This is a dynamically added paragraph text. You can replace it with any long paragraph text you want.";
});
